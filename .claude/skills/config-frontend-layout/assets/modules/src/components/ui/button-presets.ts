/**
 * Reexport dos tokens de botão definidos em `@/components/forms/form-tokens`.
 * Mantém imports existentes em `data-table`, CTAs de cadastro, etc.
 */
export { listPagePrimaryActionClassName, tablePaginationNavButtonClassName } from '@/components/forms/form-tokens';
