# Analyzer configuration. Run via `flutter analyze`.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "lib/l10n/app_localizations*.dart"

linter:
  rules:
    prefer_single_quotes: true
