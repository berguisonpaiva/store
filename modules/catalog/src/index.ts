// Module: catalog — product catalog domain (business rules only).
// Aggregates: `product` (root, owns its `variation` entities) and `category`.

export * from './product'
export * from './category'
